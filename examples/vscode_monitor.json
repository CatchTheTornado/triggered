{
  "trigger_type": "ai",
  "trigger_config": {
    "name": "vscode-monitor",
    "prompt": "Check if Visual Studio Code is running. If it is running, trigger the action. If not, don't trigger.",
    "interval": 30,
    "custom_tools_path": "./examples/process_checker.py",
    "tools": ["process_checker"]
  },
  "action_type": "shell",
  "action_config": {
    "name": "vscode-notification",
    "command": "echo 'Nice! Visual Studio Code just started'"
  }
} 